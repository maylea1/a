version: 2.0
jobs:
  deploy:
    parallelism: 3
    machine:
      enabled: true
    working_directory: /home/circleci/postgres
    environment:
      DOCKER_IMAGE=circleci/postgres-script-enhance

    steps:
    - checkout
    - setup_remote_docker
    - run: publish
    - run:
        name: Registry
        command: |
                    ls

workflows:
  version: 2
  deploy:
    jobs:
    - deploy:
        filters:
          branches:
            only: server-2.19
